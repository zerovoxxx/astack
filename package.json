{
  "name": "astack",
  "version": "1.0.3",
  "private": true,
  "description": "AI Harness System — manage Claude/Cursor/CodeBuddy skills across projects",
  "license": "MIT",
  "author": "alexjhwen",
  "packageManager": "pnpm@11.0.9+sha512.34ce82e6780233cf9cad8685029a8f81d2e06196c5a9bad98879f7424940c6817c4e4524fb7d38b8553ceed48b9758b8ebaf1abd3600c232c4c8cf7366086f38",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r --parallel run dev",
    "test": "pnpm -r run test",
    "test:coverage": "pnpm -r run test:coverage",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run lint",
    "clean": "pnpm -r run clean && rm -rf node_modules",
    "web:build": "pnpm -C packages/web build",
    "server:refresh": "pnpm -C packages/web build && (astack server stop || true) && astack server start",
    "dev:refresh": "pnpm -C packages/web build && (astack server stop || true) && astack server start",
    "dev:reload": "pnpm -r run build && (astack server stop || true) && astack server start"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^4.1.4",
    "typescript": "^5.7.2",
    "vitest": "^4.1.4"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
